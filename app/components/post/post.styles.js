import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  row:{
    flexDirection: 'row',
    flex: 1,
  },

  contentView: {
    flex: 0.8,
  },

  post: {
    borderBottomWidth: 1,
    borderColor: "#ccc",
    padding: 10,
  },

});
